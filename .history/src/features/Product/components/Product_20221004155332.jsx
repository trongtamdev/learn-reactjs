import React from 'react';
import PropTypes from 'prop-types';

Product.propTypes = {
    product: pto
};

function Product(props) {

    return (
        <div>
            Product
        </div>
    );
}

export default Product;