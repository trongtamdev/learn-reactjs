import React from 'react';
import PropTypes from 'prop-types';

ProductSketetonList.propTypes = {
    length: ptn
};

function ProductSketetonList(props) {
    return (
        <div>
            
        </div>
    );
}

export default ProductSketetonList;