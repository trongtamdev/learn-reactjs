import { Box, Container } from '@material-ui/core';

ListPage.propTypes = {
    
};

function ListPage(props) {
    return (
        <Box>
            <Container>
                <Grid
            </Container>
        </Box>
    );
}

export default ListPage;