import { Box, Container, Grid } from '@material-ui/core';

ListPage.propTypes = {
    
};

function ListPage(props) {
    return (
        <Box>
            <Container>
                <Grid container>

                </Grid>
            </Container>
        </Box>
    );
}

export default ListPage;