import React from 'react';
import PropTypes from 'prop-types';

Album.propTypes = {
    
};

function Album({}) {
    return (
        <div>
            
        </div>
    );
}

export default Album;