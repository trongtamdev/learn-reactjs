import React from 'react';
import PropTypes from 'prop-types';

Counter.propTypes = {
    
};

function Counter(props) {

    
    return (
        <div>
            
        </div>
    );
}

export default Counter;