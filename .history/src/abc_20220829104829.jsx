import React from 'react';
import PropTypes from 'prop-types';

concat.propTypes = {
    
};

function concat(props) {
    return (
        <div>
            
        </div>
    );
}

export default concat;